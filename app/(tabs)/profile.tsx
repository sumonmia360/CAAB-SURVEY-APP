import React from "react";
import { StyleSheet, Text, View } from "react-native";

const profile = () => {
  return (
    <View className="flex-1 items-center  p-3 bg-white">
      <Text className="text-dark-200 text-4xl">profile123</Text>
    </View>
  );
};

export default profile;

const styles = StyleSheet.create({});
